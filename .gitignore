# Exclude raw image assets and scripts
original_photos/
tools/

# OS files
.DS_Store
Thumbs.db

# Segredos / variáveis de ambiente — NUNCA versionar chaves secretas
.env
.env.*
supabase/.env
supabase/.env.*
**/.env.local

# Cache local da CLI Supabase (não versionar)
supabase/.temp/
supabase/.branches/

# Ferramentas de editor / harness local
.claude/

# Referências de design (handoff) — não deve ir para produção
design_handoff_homepage/
HANDOFF-*.md

# Snapshots locais para comparação (não fazem parte do site)
live_index.*

# Backups locais de imagens originais (pré-otimização)
assets/img/_backup_orig/
